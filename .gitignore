# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*-service-account*.json
credentials.json

# Krishna keeps live credentials here while the real secret manager is not yet wired.
# This must never be committed — it holds the production Neon password.
design/keys.md
**/keys.md
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml
dist/
build/
*.egg-info/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/build/
*.iml
.packages

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# npm workspace lockfile is committed; installed trees are not
package-lock.json.bak

# Stubbed OTP written by the SMS service in local development.
.otp-stub.json

# Next.js static export output
apps/web-admin/out/
apps/web-admin/.next/

# Flutter build output
apps/mobile-guard/build/
apps/mobile-guard/.dart_tool/
apps/mobile-resident/build/
apps/mobile-resident/.dart_tool/
apps/mobile-resident/.gradle/
apps/mobile-guard/.gradle/

# Scratch access token written by local smoke runs.
.token.tmp
